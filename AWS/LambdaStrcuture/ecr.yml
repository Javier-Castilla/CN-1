AWSTemplateFormatVersion: "2010-09-09"
Description: "Create ECR for Lambda"

Parameters:
  ECRName:
    Type: String
    Default: bookstore
    Description: ECR name

Resources:
  ECR:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: !Ref ECRName
      ImageScanningConfiguration:
        ScanOnPush: true
      LifecyclePolicy:
        LifecyclePolicyText: |
          {
            "rules": [
              {
                "rulePriority": 1,
                "description": "Keep only specific tags",
                "selection": {
                  "tagStatus": "tagged",
                  "tagPrefixList": ["books", "orders", "customers"],
                  "countType": "imageCountMoreThan",
                  "countNumber": 1
                },
                "action": {
                  "type": "expire"
                }
              }
            ]
          }

Outputs:
  RepositoryUri:
    Description: "ECR URI"
    Value: !GetAtt ECR.RepositoryUri
